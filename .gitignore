# Secrets
.env

# Caches
__pycache__/
*.py[cod]
.diskcache/

# Virtual environments
.venv/
venv/

# Documents (keep binaries out of the repo, except the annual reports
# committed on purpose so Render's RAG index works without a rebuild step)
*.pdf
*.ppt
*.pptx
!data/annual_reports/*.pdf

# OS / editor noise
.DS_Store
Thumbs.db

# Claude Code local state
.claude/
