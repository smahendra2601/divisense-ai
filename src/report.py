"""Tier 3 — Agentic Orchestration: Report Node output formatting.

Assembles the final markdown: direct-answer banner (when present),
forecast card, key metrics, corporate actions, the "data as of
<timestamp>" stamp, and the standard disclaimer on every output. Also
renders friendly messages for clarify, out_of_scope, and error paths —
never a stack trace.
"""
